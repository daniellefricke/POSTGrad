


<div class="container">

</div>
<% @schools.each do |school| %>
  <div class="school">
    <h2><%=link_to school.name, school_path(school)%></h2>
    <img src="<%= school.img_url %>"/>
  </div>
<% end %>
</div>
